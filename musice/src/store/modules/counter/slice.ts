import { createAction, createSlice, ActionCreator } from '@reduxjs/toolkit'

const initialState = {}

export const counterSlice = createSlice({
  name: 'counter',
  reducers: {},
  initialState: initialState
})


